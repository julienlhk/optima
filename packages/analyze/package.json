{
  "name": "@ai-opt/analyze",
  "version": "1.0.0",
  "description": "Cursor transcript forensics and session waste scoring.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ai-opt/core": "workspace:*",
    "@ai-opt/classify": "workspace:*"
  },
  "publishConfig": {
    "access": "public"
  }
}
