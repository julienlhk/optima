# Example Cursor project

Run from monorepo root:

```bash
pnpm ai-opt init --host cursor
pnpm ai-opt doctor
```

This directory can hold sample agent notes; init writes rules into the cwd you choose.
